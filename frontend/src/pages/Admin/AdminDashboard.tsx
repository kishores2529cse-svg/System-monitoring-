import React, { useState } from 'react';
import { Camera, Code2, RefreshCw, Settings2 } from 'lucide-react';
import { Navbar } from '../../components/common/Navbar';
import { Footer } from '../../components/common/Footer';
import { PageTransition } from '../../components/ui/PageTransition';
import { CreateProblemModal } from '../../components/dashboard/CreateProblemModal';
import { AdminManagementPanel } from '../../components/dashboard/AdminManagementPanel';

export const AdminDashboard: React.FC = () => {
  const [isCreateModalOpen, setIsCreateModalOpen] = useState(false);
  const [lastRefresh, setLastRefresh] = useState(new Date());
  const [cameraPolicy, setCameraPolicy] = useState(true);

  const evidence = [['Priya', 'Tab Switch', '10:20'], ['Arjun', 'Fullscreen Exit', '10:22'], ['Rahul', 'Face Missing', '10:26']];
  const activity = [['10:18', 'Rahul started exam'], ['10:20', 'Priya switched tab'], ['10:22', 'Arjun exited fullscreen'], ['10:25', 'Multiple faces detected'], ['10:27', 'Developer tools suspected']];
  return <PageTransition><div className="min-h-screen bg-[#091109] font-sans text-[#fdf8e8]"><Navbar /><main className="mx-auto w-full max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><section className="flex flex-col gap-5 rounded-3xl border border-[#FFD84D]/15 bg-gradient-to-br from-[#163013] to-[#0a1409] p-7 shadow-2xl shadow-black/20 md:flex-row md:items-end md:justify-between"><div><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.24em] text-[#7CFF4D]"><Settings2 className="h-4 w-4" />Administration workspace</div><h1 className="mt-3 text-3xl font-semibold tracking-tight text-white">Members, evidence &amp; coding problems</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">Manage candidate access, review security evidence, and publish coding problems from one workspace.</p></div><div className="flex flex-wrap gap-2"><button onClick={() => setCameraPolicy(value => !value)} className={`inline-flex items-center gap-2 rounded-xl border px-4 py-2.5 text-sm font-semibold transition ${cameraPolicy ? 'border-[#7CFF4D]/40 bg-[#173013] text-[#dfffd2]' : 'border-white/15 bg-white/5 text-slate-300'}`}><Camera className="h-4 w-4" />Camera {cameraPolicy ? 'required' : 'optional'}</button><button onClick={() => setIsCreateModalOpen(true)} className="inline-flex items-center gap-2 rounded-xl bg-[#7CFF4D] px-4 py-2.5 text-sm font-semibold text-[#091109] transition hover:bg-[#A3FF1A]"><Code2 className="h-4 w-4" />Add problem</button><button onClick={() => setLastRefresh(new Date())} className="inline-flex items-center gap-2 rounded-xl border border-white/15 bg-white/5 px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-white/10"><RefreshCw className="h-4 w-4" />Refresh</button></div></section><AdminManagementPanel /><section className="grid gap-6 lg:grid-cols-2"><div className="rounded-3xl border border-white/10 bg-black/20 p-6"><h2 className="font-semibold text-white">Evidence table</h2><p className="mt-1 text-xs text-slate-400">Recent security evidence captured during active sessions.</p><table className="mt-5 w-full text-left text-sm"><thead className="text-xs uppercase tracking-wider text-slate-400"><tr><th className="pb-3">Student</th><th>Event</th><th>Time</th></tr></thead><tbody>{evidence.map(([student, event, time]) => <tr key={`${student}-${time}`} className="border-t border-white/10"><td className="py-3 font-medium text-white">{student}</td><td><span className="rounded-full bg-[#FFD84D]/10 px-2 py-1 text-xs text-[#FFD84D]">{event}</span></td><td className="font-mono text-slate-300">{time}</td></tr>)}</tbody></table></div><div className="rounded-3xl border border-white/10 bg-black/20 p-6"><h2 className="font-semibold text-white">Live activity feed</h2><p className="mt-1 text-xs text-slate-400">Latest candidate and proctoring activity.</p><ol className="mt-5 space-y-4">{activity.map(([time, detail]) => <li key={`${time}-${detail}`} className="flex gap-4"><span className="w-11 shrink-0 font-mono text-xs text-[#7CFF4D]">{time}</span><span className="border-l border-[#FFD84D]/20 pl-4 text-sm text-slate-200">{detail}</span></li>)}</ol></div></section><p className="text-right text-xs text-slate-500">Last refreshed {lastRefresh.toLocaleTimeString()}</p></main><CreateProblemModal isOpen={isCreateModalOpen} onClose={() => setIsCreateModalOpen(false)} /><Footer /></div></PageTransition>;
};
